mixin S(nameSection, variablesSection)
	case nameSection
		when 'Test'
			if typeof pug_mixins[`${nameSection}`] !== 'function'
				include Test/Test
				+Test(variablesSection)&attributes(attributes)
			else
				+Test(variablesSection)&attributes(attributes)
