-
	function classes(cn) {
		// логика формирования классов
		const cols = cn?.cols ? cn?.cols : 'col-12';

		// определение блока 'col'
		const BLOCK = CN('col');

		return {
			col: BLOCK({}, [
				cols,
				cn?.order,
				cn?.margin,
				cn?.padding,
				cn?.display,
				cn?.justify,
				cn?.alignItems,
				cn?.direction,
				cn?.offset,
				cn?.animation,
			])
		}
	}
